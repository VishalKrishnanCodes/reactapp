# Student Login React App

Simple React app with a login form that navigates to a landing page showing "welcome {username}" and an academic year dropdown.

Run locally:

```bash
npm install
npm start
```
